# Architecture

This is a bootstrap inventory, not a claim that undocumented components exist.

## Detected shape

- Type: library or application
- Languages: TypeScript
- Frameworks: none detected
- Top-level entries: AGENTS.md, CHANGELOG.md, CLAUDE.md, companion/, companion-assets.json, docs/, e2e/, e2e-compound-real-run.log, esbuild.config.mjs, extensions/, main.js, main.ts, manifest.json, package-lock.json, package.json, packages/, playwright-report/, playwright.config.ts, playwright.public-site.config.ts, proof-matrix-state/, public-site/, README.md, scripts/, src/, styles.css, test-results/, tests/, tsconfig.json, versions.json

## Boundaries and data flow

Document verified entry points, module boundaries, external systems, data stores, and trust boundaries here as they are discovered. Do not infer them from filenames alone.

## Architectural decisions

Record material decisions and compatibility constraints here. Risky changes require human review under `.agent/constraints.md`.
