// The DU-01 scenario still depends on the mature disposable-vault lifecycle in
// the legacy native harness. This focused entrypoint is intentionally paired
// with the daily-use-note project grep so no unrelated monolith case executes.
import "./obsidian-agent.spec";
