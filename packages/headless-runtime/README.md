# Headless Runtime

Environment-neutral runtime package scaffold for authorized background work.
The versioned core API contract is supplied by the separate
`@agentic-researcher/core-api` workspace package.
