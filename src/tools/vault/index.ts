/**
 * Vault tools modularization scaffold.
 * Existing implementations remain in `../vaultTools.ts` until callers migrate.
 * New write/title/section tools should land in focused modules under this folder.
 */

export const VAULT_TOOLS_MODULE_SPLIT_PENDING = true as const;
